# This project expects secrets in GAS Script Properties, so a .env file is usually unnecessary.
# Create .env only for local tooling if needed; never commit real values.
# SYNC_GUEST_EMAILS=
# SYNC_LOOKAHEAD_DAYS=
